import { View } from "react-native";

import { Button, ButtonText } from "@acme/ui";

export default function Index() {
  return (
    <Button>
      <ButtonText>Click me</ButtonText>
    </Button>
  );
}
